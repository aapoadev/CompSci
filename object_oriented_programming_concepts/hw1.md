1. A class is a blueprint to objects and object is an instance of the class, for ex 3 is a integer object.
2. 
Train, aggregation and dependency, because train contains passengers and passengers use train
School, dependency and aggregation, because course contains students and students take/use course
Interior design inheritance, because table is in furniture class. And aggregation, because furniture has tables
Family aggregation because parent has child, and dependency, because child needs parent
Library is aggregation because book has literature and inheritance because book is literature